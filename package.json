{
  "name": "primitivepool",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pd164594/primitivepool",
    "create-local": "graph create --node http://localhost:8020/ pd164594/primitivepool",
    "remove-local": "graph remove --node http://localhost:8020/ pd164594/primitivepool",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pd164594/primitivepool"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
